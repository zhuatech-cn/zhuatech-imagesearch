/* Copyright 2026 上海如静知华信息科技有限公司 · https://www.zhuatech.cn/ */
document.querySelectorAll('.tab').forEach(button => button.onclick = () => { document.querySelectorAll('.tab,.view').forEach(el => el.classList.remove('active')); button.classList.add('active'); document.getElementById(button.dataset.view).classList.add('active'); });
const catalog=[{assetId:'IMG-24018',title:'智能制造产线巡检',source:'上海工厂',category:'factory',similarity:.928,explanation:'命中标签：工厂、产线、设备、工程师'},{assetId:'IMG-23172',title:'仓储拣货作业',source:'苏州仓',category:'warehouse',similarity:.771,explanation:'企业作业构图与设备元素接近'},{assetId:'IMG-23806',title:'研发团队产品评审',source:'产品中心',category:'meeting',similarity:.704,explanation:'人物数量与企业场景接近'}];
const button=document.getElementById('search'),query=document.getElementById('query'),topK=document.getElementById('topK'),onlyAuthorized=document.getElementById('onlyAuthorized'),results=document.getElementById('results'),meta=document.getElementById('resultMeta');
button.onclick=async()=>{button.disabled=true;button.textContent='检索中…';let data;try{const response=await fetch('/api/imagesearch/search',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:query.value,topK:+topK.value,onlyAuthorized:onlyAuthorized.checked})});if(!response.ok)throw new Error();data=await response.json()}catch(error){data={matches:catalog.slice(0,+topK.value)}}render(data.matches);setTimeout(()=>{button.disabled=false;button.textContent='开始检索'},350)};
function render(items){meta.textContent=`检索到 ${items.length} 个素材 · 本地排序`;results.innerHTML=items.map((item,index)=>`<article><div class="asset-image ${item.category||['factory','warehouse','meeting'][index%3]}"><span>${(item.similarity*100).toFixed(1)}%</span></div><div class="asset-info"><b>${item.title}</b><small>${item.assetId} · ${item.source}</small><p>${item.explanation}</p><em>授权可用</em></div></article>`).join('')}
